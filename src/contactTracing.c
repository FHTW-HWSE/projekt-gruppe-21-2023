#include <stdio.h>
#include <stdlib.h>
#include "parser.h"
#include "structhandeling.h"
#include "contact.h"


booking findBookingfromPerson(struct booking toSearch, struct person toFind){
    while(toSearch->next != NULL){
        if(toFind->id == toSeach->idPerson){

        }


        toSearch = toSearch->next;
    }
    return toSeach;

}