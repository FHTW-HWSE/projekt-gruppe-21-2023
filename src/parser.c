// TODO Header ins Include File brinngen und aufräumen

#include main.h
#include parser.h

int parsePerson(char *text, struct person *toPerson)
{
    char readString[50];
    int i = 0;

    /* do{

        do{
            toPerson->n

        }while(text[i] != ',');

    } while (text[i] != EOF); // oder bis text[i] != '/0' je nach dem wies dann im File lesen besser passt
    */
}
